# Copy to backend.hcl (gitignored) and fill in your own values:
#
#   cp backend.hcl.example backend.hcl
#   terraform init -backend-config=backend.hcl
#
# The bootstrap stack prints these for you:
#
#   terraform -chdir=../bootstrap output backend_hcl

bucket = "lizard-tfstate-000000000000"
region = "us-east-1"
